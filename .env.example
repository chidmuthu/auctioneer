# Discord
DISCORD_TOKEN=your_bot_token_here
DISCORD_GUILD_ID=your_server_id_here
# Optional: restrict auction commands to one channel (right-click channel → Copy ID). Unset = allow everywhere.
# DISCORD_AUCTION_CHANNEL_ID=1234567890123456789

# Google Sheets (POM balance + completed auctions)
# Create a Google Cloud service account, enable Sheets API, share the spreadsheet with the service account email
GOOGLE_CREDENTIALS_PATH=path/to/service-account-key.json
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
